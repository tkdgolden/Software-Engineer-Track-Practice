import { findTwoNumbers } from "./index.mjs";

describe('findTwoNumbers function', function () {
    it('test', function () {
        a = findTwoNumbers([2, 7, 11, 15], 9)
        expect(a).toEqual(a);
    });
});